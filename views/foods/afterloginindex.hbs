{{!-- home page after login --}}
{{!-- ppt page 4 --}}
{{!-- consider combine with index.hbs by using isConnected --}}
<h3>home page after login</h3>
<div class="foods-wrapper">
  {{#each foods}}
  <div class="food-item">
    <img src="{{this.imgPath}}" alt="">
      <h4>{{this.name}}</h4>
      <h4>{{this.cuisine}}</h4>
      <a href="/foods/{{this._id}}">Details</a>
      {{!-- <img src="{{}}" --}}
  </div>
  {{else}}
    <p>There are no foods yet! :(</p>
  {{/each}}
</div>